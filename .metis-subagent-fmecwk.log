# Metis v2 项目探索报告

## 1. 项目根目录结构

项目根目录包含 41 个项，包括：
- `README.md`（30KB 文档）、`CHANGELOG.md`（445KB 变更日志）
- `src/`（源代码）、`docs/`（文档）、`examples/`（示例）、`test/`（测试）
- `dist/`（构建输出）、`node_modules/`（依赖）
- 多个 `.metis-subagent-*` 日志/临时文件
- 配置文件如 `package.json`、`tsconfig.build.json`

## 2. 主要文档发现

**README.md** (30KB) 是核心文档，包含：
- 项目概述：Metis 是一个基于 Metis 终端编码框架的 AI 助手
- 快速开始指南和安装指令
- 支持多种 AI 提供商（Anthropic、OpenAI、GitHub Copilot 等 20+ 提供商）
- 四种运行模式：交互式、打印/JSON、RPC、SDK
- 丰富的自定义功能：扩展、技能、提示模板、主题
- 会话管理、分支、上下文压缩
- CLI 参考和环境变量

**文档目录** `docs/` 包含 28 个 Markdown 文件，覆盖：
- 核心功能：providers、models、sessions、settings
- 扩展机制：extensions、skills、prompt-templates、themes
- 开发指南：development、sdk、rpc
- 高级主题：compaction、security、containerization

## 3. 示例代码探索

`examples/` 目录包含 60+ 示例，分为两类</co: 4:[0]>：
- `sdk/`：12 个示例演示程序化使用（创建会话、自定义模型、技能等）
- `extensions/`：40+ 扩展示例展示：
  - 生命周期事件处理
  - 自定义工具和命令
  - Git 集成（自动提交、检查点）
  - 自定义 UI 组件（编辑器、覆盖层）
  - 外部集成（SSH、系统主题）
  - 自定义提供商实现
  - 子代理架构

## 4. 项目类型与功能分析

**项目类型**：企业级 AI 编码助手框架
- 核心是一个 Node.js 应用程序（要求 Node >= 22.19）
- 提供 CLI 工具和可编程 SDK
- 支持多种部署方式（本地、容器、云端）

**主要功能**：
- 交互式终端界面（TUI）支持丰富的键盘操作
- 内置工具：read、write、edit、bash、grep、find、ls
- 会话管理：树结构、分支、历史记录、压缩
- 上下文管理：AGENTS.md 文件、系统提示
- 扩展生态系统：TypeScript 模块可添加新功能
- 包管理：通过 npm/git 安装共享扩展
- 多模型支持：可切换不同提供商和模型
- 离线模式和自定义配置

## 5. 项目特点与用途

**特点**：
- 极简核心 + 强大扩展性
- 完全开源（MIT 许可），由 Mario Zechner 维护
- 注重开发者体验和工作流适配
- 支持实时协作和会话分享
- 包含可视化调试和监控功能
- 企业级安全性和权限控制

**用途**：
- 代码生成和审查
- 项目自动化和脚本编写
- AI 辅助编程工作流
- 自定义工具开发
- 团队知识管理
- 集成到现有开发环境
- 研究和实验新 AI 工作流程

## 6. 总结

Metis v2 是一个 成熟、功能全面的 AI 编码助手平台，而非简单工具。它通过 极简核心 + 丰富的扩展机制，让开发者 根据自身需求定制工作流，而非强制适应工具。项目文档完善，示例丰富，活跃开发（最新版本 0.80.3），适合 个人开发者团队和 企业用户构建定制化的 AI 辅助开发环境。
